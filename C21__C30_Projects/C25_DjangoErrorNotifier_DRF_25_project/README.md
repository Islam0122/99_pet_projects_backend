# 🔥 Django Error Notifier with AI Assistant

Система автоматического мониторинга и анализа ошибок Django-приложения с уведомлениями в Telegram и AI-консультантом на базе GigaChat.

## 📋 О проекте

**Django Error Notifier** — это middleware для Django, которое:
- 🚨 Автоматически отслеживает все ошибки HTTP (4xx, 5xx)
- 📱 Отправляет уведомления в Telegram в реальном времени
- 🤖 Использует GigaChat AI для анализа ошибок и предоставления рекомендаций по исправлению
- 📊 Предоставляет детальную информацию о контексте ошибки (пользователь, URL, метод, traceback)

## 🎯 Зачем нужен проект?

### Проблемы, которые решает:

1. **Мгновенное оповещение об ошибках** — узнавайте о проблемах в продакшене сразу же, не дожидаясь жалоб пользователей
2. **AI-ассистент разработчика** — получайте умные подсказки по исправлению ошибок прямо в Telegram
3. **Контекстная информация** — видите не просто traceback, а полную картину: кто, где и как вызвал ошибку
4. **Экономия времени** — не нужно копаться в логах на сервере, все приходит вам в мессенджер

### Кому подойдет:

- 👨‍💻 Разработчикам, работающим над Django-проектами
- 🏢 Командам, которым нужен быстрый мониторинг
- 🚀 Стартапам с ограниченным бюджетом (бесплатная альтернатива Sentry)
- 📚 Студентам для изучения middleware и интеграции с внешними API

## ✨ Возможности

- ⚡ **Мгновенные уведомления** о всех ошибках в Telegram
- 🧠 **AI-анализ ошибок** через GigaChat с конкретными рекомендациями
- 📝 **Детальные отчеты** с информацией о пользователе, URL, методе запроса
- 🎨 **Красивое форматирование** сообщений с эмодзи и HTML-разметкой
- 🔧 **Гибкая настройка** через Django settings
- 🌐 **REST API** с документацией (Swagger/ReDoc)
- 🎭 **Стильная админка** на базе Django Jazzmin

## 🛠 Технологии

- **Backend**: Django 5.1.3, Django REST Framework 3.15.2
- **AI**: GigaChat API (Сбер)
- **Уведомления**: Telegram Bot API
- **Документация API**: drf-spectacular
- **База данных**: SQLite (легко заменить на PostgreSQL)
- **Деплой**: Railway (готовый Procfile)

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd C25_DjangoErrorNotifier_DRF_25_project
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
DJANGO_ENV=development
SECRET_KEY=your-secret-key
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_CHAT_ID=your-chat-id
```

### 5. Получение Telegram Bot Token

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен в `.env`
4. Узнайте свой `chat_id` через [@userinfobot](https://t.me/userinfobot)

### 6. Настройка GigaChat

1. Зарегистрируйтесь на [GigaChat](https://developers.sber.ru/gigachat)
2. Получите `CLIENT_ID` и `SECRET`
3. Замените их в `apps/utils/gigachat.py`

### 7. Миграции и запуск

```bash
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

## 🚀 Использование

### Тестирование системы

Перейдите по адресу для генерации тестовой ошибки:
```
http://127.0.0.1:8000/test-500/
```

В Telegram придет:
1. 🔥 Уведомление об ошибке с полным traceback
2. 💡 Рекомендации от AI по исправлению

### Интеграция в свой проект

Middleware уже подключен в `config/settings/base.py`:

```python
MIDDLEWARE = [
    # ...
    "apps.middleware.telegram_error_middleware.TelegramErrorMiddleware",
]
```

### Настройка

В `config/settings/base.py`:

```python
TELEGRAM_BOT_TOKEN = "your-bot-token"
TELEGRAM_CHAT_ID = "your-chat-id"
TELEGRAM_NOTIFY_ENABLED = True  # Включить/выключить уведомления
```

## 📚 API документация

После запуска доступны:

- **Swagger UI**: http://127.0.0.1:8000/swagger/
- **ReDoc**: http://127.0.0.1:8000/redoc/
- **OpenAPI Schema**: http://127.0.0.1:8000/schema/

## 🎨 Админ-панель

Доступна по адресу: http://127.0.0.1:8000/admin/

Стилизована с помощью Django Jazzmin с темной темой и современным интерфейсом.

## 📂 Структура проекта

```
C25_DjangoErrorNotifier_DRF_25_project/
├── apps/
│   ├── middleware/
│   │   └── telegram_error_middleware.py  # Основной middleware
│   └── utils/
│       └── gigachat.py                   # Интеграция с GigaChat
├── config/
│   ├── settings/
│   │   ├── base.py                       # Базовые настройки
│   │   ├── development.py                # Настройки для разработки
│   │   ├── production.py                 # Настройки для продакшена
│   │   └── cors.py                       # CORS настройки
│   ├── urls.py                           # URL маршруты
│   └── wsgi.py                           # WSGI конфигурация
├── manage.py
├── requirements.txt
├── Procfile                              # Для деплоя на Railway
└── .gitignore
```

## 🌍 Деплой на Railway

1. Создайте аккаунт на [Railway](https://railway.app)
2. Подключите GitHub репозиторий
3. Добавьте переменные окружения:
   - `DJANGO_ENV=production`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
4. Railway автоматически использует `Procfile` для запуска

## 🔐 Безопасность

- ✅ Конфиденциальные данные в `.env` (не коммитятся)
- ✅ Разделение настроек на dev/prod/testing
- ✅ CORS настройки для безопасного API
- ✅ WhiteNoise для статических файлов
- ⚠️ **Важно**: Замените `SECRET_KEY` в production!

