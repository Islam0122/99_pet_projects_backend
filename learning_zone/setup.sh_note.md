# 📘 Setup.sh - Полное руководство по Bash скриптам для автоматизации

## 🎯 Что такое setup.sh?

**setup.sh** — это bash-скрипт для автоматической настройки проекта. Он выполняет все необходимые команды для развертывания проекта одной командой `./setup.sh`.

### Зачем нужен setup.sh?

✅ **Onboarding** — новый разработчик настраивает проект за 2 минуты  
✅ **Воспроизводимость** — одинаковая настройка на всех машинах  
✅ **Автоматизация** — не нужно помнить 10+ команд  
✅ **Безопасность** — меньше ошибок при ручной настройке  
✅ **CI/CD** — можно использовать в pipeline  

---

## 📋 Базовый синтаксис Bash

### Shebang (первая строка)

```bash
#!/bin/bash
```

**Что это:** Указывает операционной системе, каким интерпретатором запускать скрипт.

**Варианты:**
- `#!/bin/bash` — запуск через Bash
- `#!/bin/sh` — запуск через POSIX shell (более совместимый)
- `#!/usr/bin/env bash` — ищет bash в PATH (более портативный)

---

### Вывод текста

```bash
# Простой вывод
echo "Hello, World!"

# Вывод с переносом строки
echo -e "Line 1\nLine 2"

# Вывод без переноса строки
echo -n "Loading... "

# Цветной вывод
echo -e "\033[32mSuccess!\033[0m"  # Зеленый текст
echo -e "\033[31mError!\033[0m"    # Красный текст
```

---

### Переменные

```bash
# Объявление переменной (БЕЗ пробелов вокруг =)
NAME="John"
AGE=25

# Использование переменной
echo "Hello, $NAME"
echo "Age: ${AGE}"

# Команды в переменных
CURRENT_DIR=$(pwd)
DATE=$(date +%Y-%m-%d)
```

**❗ Важно:** Пробелы вокруг `=` приведут к ошибке!

```bash
# ❌ НЕПРАВИЛЬНО
NAME = "John"

# ✅ ПРАВИЛЬНО
NAME="John"
```

---

### Условные конструкции

```bash
# Если файл существует
if [ -f ".env" ]; then
    echo "File exists"
fi

# Если директория НЕ существует
if [ ! -d "venv" ]; then
    echo "Creating venv..."
    python3 -m venv venv
fi

# Сравнение строк
if [ "$ENV" = "production" ]; then
    echo "Production mode"
elif [ "$ENV" = "development" ]; then
    echo "Development mode"
else
    echo "Unknown environment"
fi

# Сравнение чисел
if [ $AGE -gt 18 ]; then
    echo "Adult"
fi
```

**Операторы для файлов:**
- `-f` — файл существует
- `-d` — директория существует
- `-e` — существует (файл или директория)
- `-r` — файл доступен для чтения
- `-w` — файл доступен для записи
- `-x` — файл исполняемый

**Операторы для строк:**
- `=` — равно
- `!=` — не равно
- `-z` — строка пустая
- `-n` — строка не пустая

**Операторы для чисел:**
- `-eq` — равно (equal)
- `-ne` — не равно (not equal)
- `-gt` — больше (greater than)
- `-lt` — меньше (less than)
- `-ge` — больше или равно (greater or equal)
- `-le` — меньше или равно (less or equal)

---

## 🔧 Разбор setup.sh из проекта

Давайте разберем построчно реальный `setup.sh` из вашего Django проекта:

```bash
#!/bin/bash

echo "🚀 Настройка проекта Access Control System..."

if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

echo "🔌 Активация виртуального окружения..."
source venv/bin/activate

echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

if [ ! -f ".env" ]; then
    echo "📝 Создание .env файла..."
    cp .env.example .env
    echo "⚠️  ВАЖНО: Отредактируйте .env файл с вашими настройками!"
fi

mkdir -p fixtures

echo "🗄️  Выполнение миграций..."
python manage.py makemigrations
python manage.py migrate

echo "📊 Загрузка тестовых данных..."
python manage.py loaddata fixtures/initial_data.json

echo "👥 Создание тестовых пользователей..."
python manage.py create_test_users

echo "🎨 Сбор статических файлов..."
python manage.py collectstatic --noinput

echo ""
echo "✅ Проект успешно настроен!"
echo ""
echo "📋 Тестовые пользователи:"
echo "   1. admin@example.com / admin123 (Администратор)"
echo "   2. manager@example.com / manager123 (Менеджер)"
echo "   3. user@example.com / user123 (Пользователь)"
echo "   4. guest@example.com / guest123 (Гость)"
echo ""
echo "🌐 Для запуска сервера выполните:"
echo "   python manage.py runserver"
echo ""
echo "📚 API документация будет доступна по адресу:"
echo "   http://localhost:8000/api/swagger/"
echo ""
```

---

## 📖 Построчный разбор

### 1. Shebang

```bash
#!/bin/bash
```

**Что делает:** Указывает, что скрипт должен выполняться в Bash.

---

### 2. Начальное сообщение

```bash
echo "🚀 Настройка проекта Access Control System..."
```

**Что делает:** Выводит приветственное сообщение с эмодзи.

**Эмодзи в Bash:**
- Делают вывод более читаемым
- Помогают визуально отличать этапы
- Работают в большинстве современных терминалов

---

### 3. Проверка и создание venv

```bash
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi
```

**Что делает:**
1. `[ ! -d "venv" ]` — проверяет, существует ли директория `venv`
2. `!` — отрицание (если НЕ существует)
3. Если директории нет, создает новое виртуальное окружение

**Зачем нужно:**
- Не пересоздает venv, если он уже есть
- Экономит время при повторных запусках
- Безопасно — не сломает существующий venv

---

### 4. Активация виртуального окружения

```bash
echo "🔌 Активация виртуального окружения..."
source venv/bin/activate
```

**Что делает:**
- `source` (или `.`) — выполняет команды из файла в текущей оболочке
- `venv/bin/activate` — скрипт активации venv

**Разница между `source` и `./`:**

```bash
# ✅ ПРАВИЛЬНО - активирует в текущей сессии
source venv/bin/activate

# ❌ НЕПРАВИЛЬНО - запустит в подоболочке
./venv/bin/activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Windows (Git Bash):**
```bash
source venv/Scripts/activate
```

---

### 5. Установка зависимостей

```bash
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt
```

**Что делает:**
1. Обновляет pip до последней версии
2. Устанавливает все пакеты из `requirements.txt`

**`--upgrade`** — обновляет пакет, если он уже установлен.

---

### 6. Создание .env файла

```bash
if [ ! -f ".env" ]; then
    echo "📝 Создание .env файла..."
    cp .env.example .env
    echo "⚠️  ВАЖНО: Отредактируйте .env файл с вашими настройками!"
fi
```

**Что делает:**
1. Проверяет, существует ли `.env` файл
2. Если нет — копирует шаблон из `.env.example`
3. Напоминает пользователю отредактировать настройки

**`-f`** — проверка существования файла (не директории).

---

### 7. Создание директории для fixtures

```bash
mkdir -p fixtures
```

**`-p`** (parent) — создает родительские директории, если их нет, и не выдает ошибку, если директория уже существует.

**Примеры:**

```bash
# Без -p (ошибка, если уже существует)
mkdir fixtures

# С -p (безопасно)
mkdir -p fixtures

# Создание вложенных директорий
mkdir -p path/to/nested/folder
```

---

### 8. Миграции базы данных

```bash
echo "🗄️  Выполнение миграций..."
python manage.py makemigrations
python manage.py migrate
```

**Что делает:**
1. Создает файлы миграций на основе изменений в моделях
2. Применяет миграции к базе данных

---

### 9. Загрузка тестовых данных

```bash
echo "📊 Загрузка тестовых данных..."
python manage.py loaddata fixtures/initial_data.json
```

**Что делает:** Загружает начальные данные (роли, права доступа) из JSON файла.

---

### 10. Создание тестовых пользователей

```bash
echo "👥 Создание тестовых пользователей..."
python manage.py create_test_users
```

**Что делает:** Запускает кастомную management команду для создания тестовых пользователей.

---

### 11. Сбор статических файлов

```bash
echo "🎨 Сбор статических файлов..."
python manage.py collectstatic --noinput
```

**`--noinput`** — не спрашивает подтверждения (автоматический режим).

---

### 12. Финальное сообщение

```bash
echo ""
echo "✅ Проект успешно настроен!"
echo ""
echo "📋 Тестовые пользователи:"
echo "   1. admin@example.com / admin123 (Администратор)"
```

**Пустые `echo ""`** — добавляют пустые строки для читаемости.

---

## 🚀 Расширенный setup.sh с проверками

Улучшенная версия с обработкой ошибок:

```bash
#!/bin/bash

# Цвета для вывода
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Функция для вывода ошибок
error() {
    echo -e "${RED}❌ Ошибка: $1${NC}"
    exit 1
}

# Функция для успешных сообщений
success() {
    echo -e "${GREEN}✅ $1${NC}"
}

# Функция для предупреждений
warning() {
    echo -e "${YELLOW}⚠️  $1${NC}"
}

# Функция для информации
info() {
    echo -e "${BLUE}ℹ️  $1${NC}"
}

# Заголовок
echo -e "${BLUE}"
echo "╔════════════════════════════════════════╗"
echo "║  Access Control System - Setup         ║"
echo "╚════════════════════════════════════════╝"
echo -e "${NC}"

# Проверка версии Python
info "Проверка версии Python..."
if ! command -v python3 &> /dev/null; then
    error "Python 3 не установлен"
fi

PYTHON_VERSION=$(python3 --version | cut -d " " -f 2 | cut -d "." -f 1,2)
REQUIRED_VERSION="3.8"

if [ "$(printf '%s\n' "$REQUIRED_VERSION" "$PYTHON_VERSION" | sort -V | head -n1)" != "$REQUIRED_VERSION" ]; then
    error "Требуется Python >= $REQUIRED_VERSION, установлен $PYTHON_VERSION"
fi

success "Python $PYTHON_VERSION"

# Проверка pip
info "Проверка pip..."
if ! command -v pip3 &> /dev/null; then
    error "pip не установлен"
fi
success "pip установлен"

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    info "Создание виртуального окружения..."
    python3 -m venv venv || error "Не удалось создать venv"
    success "Виртуальное окружение создано"
else
    info "Виртуальное окружение уже существует"
fi

# Активация venv
info "Активация виртуального окружения..."
source venv/bin/activate || error "Не удалось активировать venv"

# Обновление pip
info "Обновление pip..."
pip install --upgrade pip --quiet || error "Не удалось обновить pip"
success "pip обновлен"

# Установка зависимостей
info "Установка зависимостей (это может занять несколько минут)..."
pip install -r requirements.txt --quiet || error "Не удалось установить зависимости"
success "Зависимости установлены"

# Создание .env
if [ ! -f ".env" ]; then
    info "Создание .env файла..."
    if [ -f ".env.example" ]; then
        cp .env.example .env || error "Не удалось скопировать .env.example"
        warning "Не забудьте настроить .env файл!"
    else
        error "Файл .env.example не найден"
    fi
else
    info ".env файл уже существует"
fi

# Создание директорий
info "Создание необходимых директорий..."
mkdir -p fixtures logs media staticfiles

# Проверка наличия базы данных
DB_EXISTS=false
if [ -f "db.sqlite3" ]; then
    DB_EXISTS=true
    warning "База данных уже существует. Пропуск миграций."
fi

# Миграции
if [ "$DB_EXISTS" = false ]; then
    info "Выполнение миграций..."
    python manage.py makemigrations || error "Ошибка при создании миграций"
    python manage.py migrate || error "Ошибка при применении миграций"
    success "Миграции выполнены"
    
    # Загрузка fixtures
    if [ -f "fixtures/initial_data.json" ]; then
        info "Загрузка тестовых данных..."
        python manage.py loaddata fixtures/initial_data.json || warning "Не удалось загрузить fixtures"
        success "Тестовые данные загружены"
    fi
    
    # Создание тестовых пользователей
    info "Создание тестовых пользователей..."
    python manage.py create_test_users || warning "Не удалось создать тестовых пользователей"
    success "Тестовые пользователи созданы"
fi

# Сбор статики
info "Сбор статических файлов..."
python manage.py collectstatic --noinput --clear || warning "Не удалось собрать статику"
success "Статика собрана"

# Проверка конфигурации
info "Проверка конфигурации Django..."
python manage.py check || warning "Найдены проблемы в конфигурации"

# Финальное сообщение
echo ""
echo -e "${GREEN}"
echo "╔════════════════════════════════════════╗"
echo "║     ✅ Установка завершена!            ║"
echo "╚════════════════════════════════════════╝"
echo -e "${NC}"
echo ""
echo "📋 Тестовые пользователи:"
echo "   • admin@example.com / admin123 (Администратор)"
echo "   • manager@example.com / manager123 (Менеджер)"
echo "   • user@example.com / user123 (Пользователь)"
echo "   • guest@example.com / guest123 (Гость)"
echo ""
echo "🚀 Команды для запуска:"
echo "   source venv/bin/activate    # Активировать venv"
echo "   python manage.py runserver  # Запустить сервер"
echo ""
echo "📚 Документация:"
echo "   http://localhost:8000/api/swagger/"
echo "   http://localhost:8000/api/redoc/"
echo ""
echo "🔧 Дополнительные команды:"
echo "   make help                   # Список всех команд"
echo "   pytest                      # Запустить тесты"
echo ""
```

---

## 🎓 Продвинутые техники Bash

### 1. Функции

```bash
# Определение функции
greet() {
    local name=$1  # local - локальная переменная
    echo "Hello, $name!"
}

# Вызов функции
greet "John"  # Output: Hello, John!

# Функция с возвращаемым значением
add() {
    local a=$1
    local b=$2
    local result=$((a + b))
    echo $result  # Возврат через echo
}

sum=$(add 5 3)
echo "Sum: $sum"  # Output: Sum: 8
```

---

### 2. Проверка команд

```bash
# Проверка, установлена ли команда
if command -v docker &> /dev/null; then
    echo "Docker установлен"
else
    echo "Docker не установлен"
    exit 1
fi

# Более короткая версия
command -v docker &> /dev/null || { echo "Docker не установлен"; exit 1; }

# Проверка успешности выполнения
if python manage.py migrate; then
    echo "Миграции выполнены успешно"
else
    echo "Ошибка миграций"
    exit 1
fi
```

**`&> /dev/null`** — перенаправляет stdout и stderr в "черную дыру" (скрывает вывод).

---

### 3. Цветной вывод

```bash
# Определение цветов
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'  # No Color

# Использование
echo -e "${RED}Это красный текст${NC}"
echo -e "${GREEN}Это зеленый текст${NC}"
echo -e "${YELLOW}Это желтый текст${NC}"
echo -e "${BLUE}Это синий текст${NC}"

# Жирный текст
BOLD='\033[1m'
echo -e "${BOLD}Жирный текст${NC}"

# Подчеркнутый текст
UNDERLINE='\033[4m'
echo -e "${UNDERLINE}Подчеркнутый текст${NC}"
```

---

### 4. Обработка аргументов

```bash
#!/bin/bash

# Скрипт с аргументами
# Использование: ./setup.sh --env production --port 8000

while [[ $# -gt 0 ]]; do
    case $1 in
        --env)
            ENV="$2"
            shift 2
            ;;
        --port)
            PORT="$2"
            shift 2
            ;;
        --help)
            echo "Usage: $0 [--env ENV] [--port PORT]"
            exit 0
            ;;
        *)
            echo "Unknown option: $1"
            exit 1
            ;;
    esac
done

# Значения по умолчанию
ENV=${ENV:-development}
PORT=${PORT:-8000}

echo "Environment: $ENV"
echo "Port: $PORT"
```

**Использование:**
```bash
./setup.sh --env production --port 9000
```

---

### 5. Логирование

```bash
# Создание лог-файла
LOG_FILE="setup_$(date +%Y%m%d_%H%M%S).log"

# Функция логирования
log() {
    echo "[$(date '+%Y-%m-%d %H:%M:%S')] $1" | tee -a "$LOG_FILE"
}

# Использование
log "Начало установки"
log "Установка зависимостей..."
pip install -r requirements.txt 2>&1 | tee -a "$LOG_FILE"
log "Установка завершена"
```

**`tee -a`** — записывает в файл И выводит на экран.

---

### 6. Прогресс-бар

```bash
# Простой прогресс-бар
show_progress() {
    local duration=$1
    local steps=50
    
    for ((i=0; i<=steps; i++)); do
        local percent=$((i * 100 / steps))
        local filled=$((i * 50 / steps))
        local empty=$((50 - filled))
        
        printf "\r["
        printf "%${filled}s" | tr ' ' '█'
        printf "%${empty}s" | tr ' ' '░'
        printf "] %3d%%" "$percent"
        
        sleep $(echo "scale=2; $duration / $steps" | bc)
    done
    echo ""
}

echo "Установка зависимостей..."
show_progress 3  # 3 секунды
```

---

### 7. Интерактивный ввод

```bash
# Запрос подтверждения
read -p "Вы уверены? (yes/no): " answer
if [ "$answer" = "yes" ]; then
    echo "Продолжаем..."
else
    echo "Отменено"
    exit 0
fi

# Скрытый ввод (для паролей)
read -s -p "Введите пароль: " password
echo ""  # Перенос строки

# Выбор из списка
PS3="Выберите окружение: "
options=("development" "production" "testing")
select opt in "${options[@]}"; do
    case $opt in
        "development")
            ENV="development"
            break
            ;;
        "production")
            ENV="production"
            break
            ;;
        "testing")
            ENV="testing"
            break
            ;;
        *)
            echo "Неверный выбор"
            ;;
    esac
done
```

---

### 8. Работа с файлами

```bash
# Чтение файла построчно
while IFS= read -r line; do
    echo "Line: $line"
done < requirements.txt

# Проверка размера файла
file_size=$(stat -f%z "db.sqlite3" 2>/dev/null || stat -c%s "db.sqlite3" 2>/dev/null)
if [ "$file_size" -gt 10485760 ]; then  # 10 MB
    echo "База данных больше 10 MB"
fi

# Поиск и замена в файле
sed -i 's/DEBUG=True/DEBUG=False/g' .env  # Linux
sed -i '' 's/DEBUG=True/DEBUG=False/g' .env  # macOS
```

---

### 9. Обработка ошибок (trap)

```bash
#!/bin/bash

# Очистка при выходе
cleanup() {
    echo "Очистка временных файлов..."
    rm -rf /tmp/my_temp_*
}

# Регистрация функции очистки
trap cleanup EXIT

# Обработка прерывания (Ctrl+C)
trap 'echo "Скрипт прерван!"; exit 130' INT

echo "Скрипт запущен..."
sleep 10
echo "Скрипт завершен"
```

---

### 10. Параллельное выполнение

```bash
# Запуск команд в фоне
echo "Запуск задач..."

task1() {
    echo "Task 1 started"
    sleep 2
    echo "Task 1 finished"
}

task2() {
    echo "Task 2 started"
    sleep 3
    echo "Task 2 finished"
}

task1 &
task2 &

# Ожидание завершения всех фоновых задач
wait

echo "Все задачи завершены"
```

---

## 🛠️ Полезные команды и паттерны

### Проверка ОС

```bash
if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    echo "Linux"
elif [[ "$OSTYPE" == "darwin"* ]]; then
    echo "macOS"
elif [[ "$OSTYPE" == "msys" || "$OSTYPE" == "cygwin" ]]; then
    echo "Windows"
fi
```

---

### Проверка root прав

```bash
if [ "$EUID" -ne 0 ]; then
    echo "Запустите с sudo"
    exit 1
fi
```

---

### Получение директории скрипта

```bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
echo "Скрипт находится в: $SCRIPT_DIR"
```

---

### Timeout для команд

```bash
# Завершить команду через 30 секунд
timeout 30s python manage.py migrate || echo "Timeout!"
```

---

## 🐛 Частые ошибки

### 1. ❌ Забыли сделать исполняемым

```bash
# Ошибка
./setup.sh
# bash: ./setup.sh: Permission denied

# Решение
chmod +x setup.sh
./setup.sh
```

---

### 2. ❌ Windows line endings (CRLF)

Если скрипт создан в Windows:

```bash
# Ошибка
-bash: ./setup.sh: /bin/bash^M: bad interpreter

# Решение
dos2unix setup.sh  # Конвертирует CRLF в LF
```

---

### 3. ❌ Пробелы в условиях

```bash
# ❌ НЕПРАВИЛЬНО
if [$VAR = "test"]; then

# ✅ ПРАВИЛЬНО
if [ "$VAR" = "test" ]; then
```

---

### 4. ❌ Кавычки в переменных

```bash
# Проблема с пробелами
FILE="my file.txt"

# ❌ Ошибка - воспринимает как два аргумента
rm $FILE

# ✅ Правильно
rm "$FILE"
```

---

## 📚 Best Practices

### 1. Всегда используйте `set -e`

```bash
#!/bin/bash
set -e  # Прервать при первой ошибке

python manage.py migrate  # Если упадет, скрипт остановится
python manage.py collectstatic
```

**Опции `set`:**
- `set -e` — прервать при ошибке
- `set -u` — ошибка при использовании неинициализированной переменной
- `set -x` — выводить каждую команду перед выполнением (для отладки)
- `set -o pipefail` — учитывать ошибки в pipeline

**Комбинация:**
```bash
#!/bin/bash
set -euo pipefail
```

---

### 2. Проверяйте зависимости

```bash
check_dependency() {
    if ! command -v "$1" &> /dev/null; then
        echo "Ошибка: $1 не установлен"
        exit 1
    fi
}

check_dependency python3
check_dependency pip3
check_dependency docker
```

---

### 3. Используйте переменные для путей

```bash
PROJECT_DIR="$(pwd)"
VENV_DIR="$PROJECT_DIR/venv"
LOG_DIR="$PROJECT_DIR/logs"

mkdir -p "$LOG_DIR"
source "$VENV_DIR/bin/activate"
```

---

### 4. Документируйте скрипт

```bash
#!/bin/bash
#
# Setup script for Access Control System
#
# Usage:
#   ./setup.sh [--skip-venv] [--skip-fixtures]
#
# Options:
#   --skip-venv      Don't create virtual environment
#   --skip-fixtures  Don't load initial data
#
# Author: Your Name
# Date: 2024-01-01
```

