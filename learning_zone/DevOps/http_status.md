
# HTTP — основы веб-разработки

HTTP (HyperText Transfer Protocol) — протокол обмена данными между клиентом и сервером.

Клиент: браузер, мобильное приложение, frontend  
Сервер: backend (Django, FastAPI, Node.js и т.д.)

---

## 🔁 Как работает HTTP

1. Клиент отправляет HTTP-запрос
2. Сервер обрабатывает запрос
3. Сервер возвращает HTTP-ответ

HTTP — **stateless**
➡️ сервер не хранит состояние между запросами  
➡️ для состояния используют cookies, sessions, tokens (JWT)

---

## 📤 HTTP-запрос (Request)

Состоит из:
- Method
- URL
- Headers
- Body (не всегда)

### Пример запроса
```http
POST /api/login HTTP/1.1
Host: example.com
Content-Type: application/json
Authorization: Bearer token

{
  "email": "user@mail.com",
  "password": "123456"
}


---

📥 HTTP-ответ (Response)

Состоит из:

Status Code

Headers

Body


Пример ответа

HTTP/1.1 200 OK
Content-Type: application/json

{
  "id": 1,
  "name": "Islam"
}


---

🔧 HTTP методы (кратко)

GET — получить данные

POST — создать

PUT — обновить полностью

PATCH — обновить частично

DELETE — удалить

OPTIONS — доступные методы

HEAD — только заголовки



---

🚦 HTTP статусы (кратко)

2xx — успех

3xx — редирект

4xx — ошибка клиента

5xx — ошибка сервера


Примеры:

200 OK

201 Created

400 Bad Request

401 Unauthorized

403 Forbidden

404 Not Found

422 Validation Error

500 Internal Server Error



---

🧾 HTTP Headers — зачем нужны

Передают мета-информацию

Часто используемые:

Content-Type

Authorization

Accept

User-Agent

Cookie

Set-Cookie



---

🔐 HTTP и безопасность

HTTP — небезопасный

HTTPS = HTTP + TLS (шифрование)


📌 Всегда используй HTTPS


---

🌐 HTTP версии

HTTP/1.1 — классика

HTTP/2 — быстрее, мультиплексирование

HTTP/3 — поверх QUIC (UDP)



---

🧠 Где используется HTTP

REST API

GraphQL

OAuth

Microservices

Webhooks

Mobile apps

SPA (React, Vue)



---

❌ Частые ошибки

Использовать GET для изменения данных

Всегда возвращать 200

Не использовать статусы

Передавать токены в URL

Игнорировать HTTPS



---




# HTTP статусы в веб-разработке

HTTP статус-коды показывают результат обработки запроса сервером.
Они делятся на 5 основных групп.

---

## 1xx — Информационные
Редко используются напрямую в приложениях.

- **100 Continue** — сервер получил заголовки, можно отправлять тело
- **101 Switching Protocols** — смена протокола (например, WebSocket)

---

## 2xx — Успешные

- **200 OK**
  - Запрос выполнен успешно
  - Используется при `GET`, `PUT`, иногда `POST`

- **201 Created**
  - Ресурс успешно создан
  - Часто используется при `POST`
  - Хорошая практика — вернуть созданный объект

- **202 Accepted**
  - Запрос принят, но обработка ещё идёт (асинхронно)

- **204 No Content**
  - Успех, но без тела ответа
  - Часто при `DELETE`

---

## 3xx — Перенаправления

- **301 Moved Permanently**
  - Ресурс перемещён навсегда
  - Важно для SEO

- **302 Found**
  - Временное перенаправление

- **304 Not Modified**
  - Данные не изменились (кеширование)

---

## 4xx — Ошибки клиента

- **400 Bad Request**
  - Неверные данные запроса
  - Часто: ошибки валидации

- **401 Unauthorized**
  - Пользователь не авторизован
  - Нет или неверный токен

- **403 Forbidden**
  - Доступ запрещён
  - Пользователь авторизован, но нет прав

- **404 Not Found**
  - Ресурс не найден

- **405 Method Not Allowed**
  - HTTP-метод не поддерживается (`POST` вместо `GET`)

- **409 Conflict**
  - Конфликт данных (например, дубликат)

- **422 Unprocessable Entity**
  - Ошибка бизнес-логики / валидации
  - Часто используется в REST API

---

## 5xx — Ошибки сервера

- **500 Internal Server Error**
  - Общая ошибка сервера
  - Часто из-за багов в коде

- **502 Bad Gateway**
  - Сервер получил неверный ответ от другого сервиса

- **503 Service Unavailable**
  - Сервер временно недоступен
  - Перегрузка или техработы

- **504 Gateway Timeout**
  - Сервер не дождался ответа от другого сервиса

---

## Практика для API

| Сценарий | Статус |
|--------|--------|
| Успешный GET | 200 |
| Создание объекта | 201 |
| Удаление | 204 |
| Ошибка валидации | 400 / 422 |
| Нет токена | 401 |
| Нет прав | 403 |
| Не найдено | 404 |
| Ошибка сервера | 500 |

---

## Хорошие практики

- Используй **201** при создании ресурсов
- Не возвращай **200** при ошибках
- **422** лучше, чем 400 для валидации
- Всегда возвращай понятное сообщение об ошибке

---

## Пример ответа API

```json
{
  "error": "ValidationError",
  "message": "Email is required"
}
```

# HTTP методы в веб-разработке

HTTP метод определяет, **что именно клиент хочет сделать** с ресурсом на сервере.

---

## 🔹 GET
Получение данных (чтение)

- Не изменяет данные
- Можно кэшировать
- Параметры передаются в URL

Примеры:
- Получить список пользователей
- Получить детальную информацию

```http
GET /api/users/
GET /api/users/42/

```
📌 Должен быть idempotent (одинаковый результат при повторе)
---

🔹 POST

Создание ресурса

Изменяет состояние сервера

Тело запроса содержит данные

Не idempotent


POST /api/users/

📌 Обычно возвращает 201 Created


---

🔹 PUT

Полное обновление ресурса

Заменяет ресурс целиком

Требует все поля


PUT /api/users/42/

📌 Idempotent


---

🔹 PATCH

Частичное обновление ресурса

Обновляет только переданные поля

Чаще используется, чем PUT


PATCH /api/users/42/

📌 Предпочтителен для REST API


---

🔹 DELETE

Удаление ресурса

DELETE /api/users/42/

📌 Часто возвращает 204 No Content


---

🔹 HEAD

Получение только заголовков

Без тела ответа

Проверка существования ресурса


HEAD /api/users/42/


---

🔹 OPTIONS

Информация о доступных методах

Используется браузером (CORS)

Возвращает Allow


OPTIONS /api/users/


---

🧠 Таблица соответствия CRUD

Действие	Метод

Read	GET
Create	POST
Update (full)	PUT
Update (partial)	PATCH
Delete	DELETE



---

⚙️ Рекомендации

Не используй GET для изменений

POST — только для создания

PATCH лучше PUT в API

DELETE не должен возвращать тело

OPTIONS и HEAD не трогай вручную



---

🚀 Примеры правильных связок

Метод	Статус

GET	200
POST	201
PATCH	200 / 204
DELETE	204
Ошибка валидации	422



---

❌ Частые ошибки

POST вместо PATCH

GET с побочными эффектами

PUT без всех полей

200 OK при ошибке


---